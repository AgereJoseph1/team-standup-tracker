# Team Standup Tracker Backend

FastAPI backend for tracking daily standups, teams, and reporting.

## Development

```bash
cd backend
poetry install
poetry run uvicorn app.main:app --reload
```

## Testing

```bash
cd backend
poetry run pytest
```
